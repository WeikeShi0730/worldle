export interface CountryType {
  value: string;
  latitude: number;
  longitude: number;
  label: string;
}

// export interface CountryType {
//   value: string;
//   label: string;
// }

export interface differenceType {
  distance: number;
  direction: string;
}
